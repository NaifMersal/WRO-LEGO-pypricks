---
title: "One Library, Three Engines"
subtitle: "Your seven verbs written three ways — motor.run() · motor.dc() · DriveBase"
format:
  html:
    embed-resources: true
    toc: true
    toc-depth: 2
    code-copy: true
    page-layout: article
resources:
  - code/compare/my_lib_run.py
  - code/compare/my_lib_dc.py
  - code/compare/my_lib_drivebase.py
---

<!-- KEEP IN SYNC with code/compare/*.py — the code blocks below are pasted copies. -->

```{=html}
<style>
:root{ --navy:#1C355E; --teal:#00AE8D; --coral:#E96852; }
h1, .title{ color:var(--navy); }
h2{ color:var(--navy); border-bottom:3px solid var(--teal); padding-bottom:.15em; }
.panel-tabset .nav-link.active{ color:var(--navy); border-bottom:3px solid var(--coral); font-weight:700; }
.engine-tag{ display:inline-block; padding:.1em .6em; border-radius:999px; background:var(--teal); color:#fff; font-size:.8em; font-weight:700; }
</style>
```

## The deal

One library. Seven verbs. **Three engines.** Every function below has the *same name, same
inputs, same promise* in all three files — only the engine underneath changes. Diff them line
by line and you'll see exactly what each layer of Pybricks does for you.

| Verb | The promise |
|---|---|
| `reset_heading(heading_deg=0)` | Zero (or set) the gyro heading frame |
| `drive_straight(distance_mm, speed=STRAIGHT_SPEED)` | Gyro-held straight; negative = backward |
| `turn_to(heading_deg)` | Turn to an **absolute** heading |
| `drive_to_wall(speed=APPROACH_SPEED, timeout_ms=4000)` | Wall touch, no distance sensor — `True` on stall |
| `wall_square(new_heading=0, backoff=BACKOFF_MM)` | Press square, reset gyro, back off |
| `follow_line(sensor, distance_mm, speed=LINE_SPEED)` | PD edge follow for a distance |
| `square_on_line(speed=CREEP_SPEED)` | Per-wheel creep — pivot square onto the line |

**Units are the contract:** distances in **mm**, speeds in **mm/s**, headings in **absolute
degrees**. Same in all three files — that's what makes them swappable.

> 🧭 **`turn_to` is a compass, not a steering wheel.** It takes the heading you want to *face*,
> not the amount you want to *spin*. `turn_to(90)` three times leaves you at 90 — not 270. That
> is the whole point: every `turn_to` throws away the leftover error from the last move instead
> of inheriting it, so mistakes can't pile up across a run. There is deliberately **no** "turn
> by 90 from here" verb — that's the one that lets error compound.

**Every default lives at the top of the file.** `speed=STRAIGHT_SPEED` isn't a magic number
hidden in the code — it's the constant in that file's *robot facts* block. Change the robot,
change one line, every verb follows.

> 🎢 **The arc you just lived.** D3–D7 you *hand-built* the `run()` version — that's the middle
> tab everywhere below. At **D8** Pybricks hands you `DriveBase`, the earned shortcut. And
> `dc()`? That's the trapdoor **under** both: raw motor power, nothing for free. Reading it is
> how you find out what `run()` was quietly doing for you all along.

Full copies (and downloads) are at the [bottom of this page](#the-full-files-copy-me).

## What each engine gives you for free

| You need… | `dc()` | `run()` | `DriveBase` |
|---|:---:|:---:|:---:|
| mm → wheel degrees | 🛠️ you | 🛠️ you | ✅ free |
| Speed that survives battery + load | ❌ duty ≠ speed | ✅ free | ✅ free |
| Acceleration ramp (no wheel chirp) | 🛠️ you (`RAMP_STEP`) | ✅ free | ✅ free (`settings`) |
| Two wheels driving straight together | 🛠️ you (gyro × KP) | 🛠️ you (gyro × KP) | ✅ free (`use_gyro`) |
| Stall detection ("that's a wall") | 🛠️ you (watch the encoder) | ✅ `motor.stalled()` | ✅ `drive.stalled()` |
| Driving ONE wheel by itself | ✅ | ✅ | ❌ no per-wheel verb |

> 🔋 **duty ≠ speed.** `dc(40)` on a fresh battery, a tired battery, and a carpet seam is
> *three different robots*. `run(400)` is the same robot every time — its controller spends
> whatever duty it takes to hold 400 deg/s. That controller is what you give up when you
> drop to `dc()` — and what `DriveBase` doubles down on.

Count the **engine-compensation** knobs — constants that exist only to tame the raw engine:
`my_lib_dc.py` carries a dozen, `my_lib_run.py` a handful, `my_lib_drivebase.py` needs **zero**
(its `drive.settings(...)` and the pro controller do that job). The robot facts and the shared
line gains (`KP_LINE`/`KD_LINE`) sit at the top of every file — tune them per robot, right there.

*(`reset_heading` is the same one-liner in all three files — `hub.imu.reset_heading(heading_deg)` —
so it doesn't get its own section below.)*

## drive_straight

The heart of the library. All three keep the robot straight the same way — *watch the gyro,
correct with the wheels* — but look at what each engine makes you carry to get there.

::: {.panel-tabset group="engine"}

### run()

```python
def drive_straight(distance_mm, speed=STRAIGHT_SPEED):
    """Gyro-held straight drive. Negative distance = backward."""
    wheel_speed = mmps_to_degps(speed)
    sign = 1 if distance_mm >= 0 else -1
    target_deg = mm_to_deg(abs(distance_mm))
    left_motor.reset_angle(0)
    right_motor.reset_angle(0)
    heading0 = hub.imu.heading()
    while (abs(left_motor.angle()) + abs(right_motor.angle())) / 2 < target_deg:
        steer = KP_HEAD * (hub.imu.heading() - heading0)  # speed = error x KP
        left_motor.run(sign * wheel_speed - steer)
        right_motor.run(sign * wheel_speed + steer)
        wait(10)
    left_motor.hold()
    right_motor.hold()
```

### dc()

```python
def drive_straight(distance_mm, speed=STRAIGHT_SPEED):
    """Gyro-held straight drive. Negative distance = backward."""
    base = min(MAX_DUTY, max(MIN_DUTY, mmps_to_duty(speed)))
    sign = 1 if distance_mm >= 0 else -1
    target_deg = mm_to_deg(abs(distance_mm))
    left_motor.reset_angle(0)
    right_motor.reset_angle(0)
    heading0 = hub.imu.heading()
    duty = 0
    while (abs(left_motor.angle()) + abs(right_motor.angle())) / 2 < target_deg:
        duty = min(base, duty + RAMP_STEP)  # ramp up — no ramp = wheel chirp
        steer = KP_HEAD * (hub.imu.heading() - heading0)  # duty = error x KP
        # flip steer's sign if the robot fights its own correction  TUNE
        left_motor.dc(sign * duty - steer)
        right_motor.dc(sign * duty + steer)
        wait(10)
    left_motor.brake()
    right_motor.brake()
```

### DriveBase

```python
def drive_straight(distance_mm, speed=STRAIGHT_SPEED):
    """Gyro-held straight drive. Negative distance = backward."""
    drive.settings(straight_speed=speed)
    drive.straight(distance_mm)
```

:::

> 👀 **What to notice.** run() and dc() are *the same loop* — same encoder stop, same
> `error × KP` steer. The dc() version just has two extra jobs: guess a duty from the speed
> (`mmps_to_duty` — a guess that drifts with the battery) and ramp it up by hand
> (`RAMP_STEP`), because raw duty from a standstill chirps the wheels and shakes the gyro.
> DriveBase? The whole loop *is* `drive.straight(distance_mm)`.

## turn_to

One verb, and it's **absolute**: "face 90," never "spin 90 more." The error is *degrees still
to turn*, and it shrinks as you go. Watch which engine gets absolute for free — and which one
has to build it.

::: {.panel-tabset group="engine"}

### run()

```python
def turn_to(heading_deg):
    """Turn TO an absolute heading — a compass, not a steering wheel.
    turn_to(90) twice leaves you at 90, not 180."""
    while True:
        error = heading_deg - hub.imu.heading()
        if abs(error) < TURN_TOL:
            break
        # Speed SHRINKS with the error — big error turns fast, small error
        # creeps in, so the robot never coasts past the target.
        speed = min(MAX_TURN_SPEED, max(MIN_TURN_SPEED, KP_TURN * abs(error)))
        if error > 0:                       # positive heading = clockwise
            left_motor.run(speed)
            right_motor.run(-speed)
        else:
            left_motor.run(-speed)
            right_motor.run(speed)
        wait(5)
    left_motor.hold()
    right_motor.hold()
```

### dc()

```python
def turn_to(heading_deg):
    """Turn TO an absolute heading — a compass, not a steering wheel.
    turn_to(90) twice leaves you at 90, not 180."""
    while True:
        error = heading_deg - hub.imu.heading()
        if abs(error) < TURN_TOL:
            break
        # MIN_DUTY floor: pure error x KP fades below static friction near the
        # target and the turn stalls a few degrees short — forever. The floor
        # is the honest fix (no I-term in this bootcamp).
        duty = min(MAX_DUTY, max(MIN_DUTY, KP_TURN * abs(error)))
        if error > 0:                       # positive heading = clockwise
            left_motor.dc(duty)
            right_motor.dc(-duty)
        else:
            left_motor.dc(-duty)
            right_motor.dc(duty)
        wait(5)
    left_motor.brake()
    right_motor.brake()
    wait(150)                               # settle — brake is not hold
```

### DriveBase

```python
def turn_to(heading_deg):
    """Turn TO an absolute heading — a compass, not a steering wheel.
    turn_to(90) twice leaves you at 90, not 180.

    drive.turn() is RELATIVE — it turns BY the angle you hand it. So the
    subtraction below is what makes this verb absolute. Without it the turns
    pile up and turn_to(90) three times ends at 270."""
    drive.turn(heading_deg - hub.imu.heading())
```

:::

> 👀 **What to notice.** **Read `drive.turn()` carefully — it is *relative*.** It turns the
> robot **by** the angle you hand it, from wherever it happens to be pointing. So the
> subtraction, `heading_deg - hub.imu.heading()`, is not decoration — it is the entire
> difference between "face 90" and "spin 90 more." Delete it and every turn silently adds to
> the last one: `turn_to(90)` three times ends at 270 and the robot walks off the mat while
> the code still *looks* right. The hand-rolled loop can't make that mistake — it chases the
> error until it dies, so it's born absolute.
>
> Only dc() needs `MIN_DUTY` — why? Because as the error shrinks, `error × KP` shrinks with
> it, and below ~12% duty static friction wins: the turn stalls 5° short and sits there
> forever. run() has the same shrinking output but its controller *delivers* small speeds
> honestly, so a floor speed (`MIN_TURN_SPEED`) is enough. DriveBase folds deceleration,
> floor, and stop into that one line.

## drive_to_wall

No distance sensor on this robot — **the motor is the sensor** (D7). Watch how "am I stalled?"
changes meaning across the engines.

::: {.panel-tabset group="engine"}

### run()

```python
def drive_to_wall(speed=APPROACH_SPEED, timeout_ms=4000):
    """Wall touch WITHOUT a distance sensor. True on stall, False on timeout."""
    wheel_speed = mmps_to_degps(speed)
    watch = StopWatch()
    left_motor.run(wheel_speed)
    right_motor.run(wheel_speed)
    while watch.time() < timeout_ms:
        # run() keeps the speed controller on, so stalled() works for free —
        # the controller knows it can't reach its target. (dc() can't do this.)
        if (watch.time() > STALL_GRACE_MS
                and left_motor.stalled() and right_motor.stalled()):
            left_motor.stop()
            right_motor.stop()
            return True
        wait(10)
    left_motor.stop()
    right_motor.stop()
    return False
```

### dc()

```python
def drive_to_wall(speed=APPROACH_SPEED, timeout_ms=4000):
    """Wall touch WITHOUT a distance sensor. True on stall, False on timeout."""
    duty = max(MIN_DUTY, mmps_to_duty(speed))
    watch = StopWatch()
    left_motor.dc(duty)
    right_motor.dc(duty)
    while watch.time() < timeout_ms:
        # stalled() means nothing under dc() — no controller, no target.
        # The encoder is the sensor: measured speed collapsing = the wall.
        if (watch.time() > STALL_GRACE_MS
                and abs(left_motor.speed()) < STALL_SPEED
                and abs(right_motor.speed()) < STALL_SPEED):
            left_motor.brake()
            right_motor.brake()
            return True
        wait(10)
    left_motor.brake()
    right_motor.brake()
    return False
```

### DriveBase

```python
def drive_to_wall(speed=APPROACH_SPEED, timeout_ms=4000):
    """Wall touch WITHOUT a distance sensor. True on stall, False on timeout."""
    watch = StopWatch()
    drive.drive(speed, 0)
    while watch.time() < timeout_ms:
        # stall detection: built in. The grace window still matters — a
        # standing start looks exactly like a wall for the first moment.
        if watch.time() > STALL_GRACE_MS and drive.stalled():
            drive.stop()
            return True
        wait(10)
    drive.stop()
    return False
```

:::

> 👀 **What to notice.** The stall-detection ladder: dc() hand-rolls a watchdog
> (`abs(motor.speed()) < STALL_SPEED` — the encoder still measures even when nothing
> controls) · run() gets `motor.stalled()` free, because a controller that can't reach its
> target *knows* it's stuck · DriveBase gets `drive.stalled()` for the whole robot. All
> three need the grace period — a standing start looks exactly like a wall.

## wall_square

The anchor run's position-reset primitive: press flat, trust the wall, re-zero the gyro.

::: {.panel-tabset group="engine"}

### run()

```python
def wall_square(new_heading=0, backoff=BACKOFF_MM):
    """Square onto a wall, reset the gyro, back off a measured distance."""
    drive_to_wall()
    wait(250)                               # settle flat against the wall
    reset_heading(new_heading)
    drive_straight(-backoff)
```

### dc()

```python
def wall_square(new_heading=0, backoff=BACKOFF_MM):
    """Square onto a wall, reset the gyro, back off a measured distance."""
    duty = max(MIN_DUTY, mmps_to_duty(APPROACH_SPEED))
    watch = StopWatch()
    left_motor.dc(duty)
    right_motor.dc(duty)
    left_done = right_done = False
    while not (left_done and right_done):
        if watch.time() > 4000:
            break
        if watch.time() > STALL_GRACE_MS:
            # Each wheel that reaches the wall drops to a gentle lean so it
            # keeps pressing while the other wheel catches up = square.
            if not left_done and abs(left_motor.speed()) < STALL_SPEED:
                left_motor.dc(PRESS_DUTY)
                left_done = True
            if not right_done and abs(right_motor.speed()) < STALL_SPEED:
                right_motor.dc(PRESS_DUTY)
                right_done = True
        wait(10)
    wait(250)                               # settle flat against the wall
    reset_heading(new_heading)
    left_motor.brake()
    right_motor.brake()
    drive_straight(-backoff)
```

### DriveBase

```python
def wall_square(new_heading=0, backoff=BACKOFF_MM):
    """Square onto a wall, reset the gyro, back off a measured distance."""
    drive_to_wall()
    wait(250)                               # settle flat against the wall
    reset_heading(new_heading)
    drive_straight(-backoff)
```

:::

> 👀 **What to notice.** run() and DriveBase get this almost for free: a stalled `run()`
> keeps *pushing* at its duty limit, so both wheels naturally stay pressed while the robot
> settles square. Under dc() nothing keeps pressing for you — the dc version tracks each
> wheel separately and drops the arrived wheel to a gentle `PRESS_DUTY` lean so the late
> wheel can swing the robot flat.

## follow_line

The D7 PD controller. Find the one line all three share — it's the whole idea.

::: {.panel-tabset group="engine"}

### run()

```python
def follow_line(sensor, distance_mm, speed=LINE_SPEED):
    """PD line follow on one sensor edge for an encoder distance."""
    base = mmps_to_degps(speed)
    target_deg = mm_to_deg(abs(distance_mm))
    left_motor.reset_angle(0)
    right_motor.reset_angle(0)
    last_error = 0
    while (abs(left_motor.angle()) + abs(right_motor.angle())) / 2 < target_deg:
        error = sensor.reflection() - LINE_THRESHOLD
        steer = KP_LINE * error + KD_LINE * (error - last_error)  # PD: P + D
        last_error = error
        left_motor.run(base + steer)
        right_motor.run(base - steer)
        wait(10)
    left_motor.hold()
    right_motor.hold()
```

### dc()

```python
def follow_line(sensor, distance_mm, speed=LINE_SPEED):
    """PD line follow on one sensor edge for an encoder distance."""
    base = max(MIN_DUTY, mmps_to_duty(speed))
    target_deg = mm_to_deg(abs(distance_mm))
    left_motor.reset_angle(0)
    right_motor.reset_angle(0)
    last_error = 0
    while (abs(left_motor.angle()) + abs(right_motor.angle())) / 2 < target_deg:
        error = sensor.reflection() - LINE_THRESHOLD
        steer = KP_LINE * error + KD_LINE * (error - last_error)  # PD — raw duty:
        last_error = error                  # visibly slows on seams (duty != speed)
        left_motor.dc(min(MAX_DUTY, max(-MAX_DUTY, base + steer)))
        right_motor.dc(min(MAX_DUTY, max(-MAX_DUTY, base - steer)))
        wait(10)
    left_motor.brake()
    right_motor.brake()
```

### DriveBase

```python
def follow_line(sensor, distance_mm, speed=LINE_SPEED):
    """PD line follow on one sensor edge for an encoder distance."""
    drive.reset()
    last_error = 0
    while abs(drive.distance()) < abs(distance_mm):
        error = sensor.reflection() - LINE_THRESHOLD
        steer = KP_LINE * error + KD_LINE * (error - last_error)  # PD: P + D
        last_error = error
        drive.drive(speed, steer)
        wait(10)
    drive.stop()
```

:::

> 👀 **What to notice.** `error = sensor.reflection() - LINE_THRESHOLD` then
> `error × KP + KD × Δerror` — identical in all three tabs. That *is* the PD controller: P
> steers on how far off the edge you are, D reacts to how *fast* that error is changing and
> calms the wiggle. The engine only changes what the output *means* (wheel speed · raw duty ·
> steering rate). The dc() follower is the honest one to watch on the mat: it visibly slows on
> carpet seams because duty ≠ speed. Same code, three KP/KD pairs — the *units* change with
> the engine.

## square_on_line

The plot twist.

::: {.panel-tabset group="engine"}

### run()

```python
def square_on_line(speed=CREEP_SPEED):
    """Creep until EACH eye sees the line, holding that side's wheel — pivots square."""
    creep = mmps_to_degps(speed)
    left_done = right_done = False
    left_motor.run(creep)
    right_motor.run(creep)
    while not (left_done and right_done):
        if not left_done and left_eye.reflection() < LINE_THRESHOLD:
            left_motor.hold()
            left_done = True
        if not right_done and right_eye.reflection() < LINE_THRESHOLD:
            right_motor.hold()
            right_done = True
        wait(5)
```

### dc()

```python
def square_on_line(speed=CREEP_SPEED):
    """Creep until EACH eye sees the line, holding that side's wheel — pivots square."""
    creep = max(MIN_DUTY, mmps_to_duty(speed))
    left_done = right_done = False
    left_motor.dc(creep)
    right_motor.dc(creep)
    while not (left_done and right_done):
        if not left_done and left_eye.reflection() < LINE_THRESHOLD:
            left_motor.hold()               # hold() is a controller command —
            left_done = True                # it works fine even from dc()
        if not right_done and right_eye.reflection() < LINE_THRESHOLD:
            right_motor.hold()
            right_done = True
        wait(5)
```

### DriveBase

```python
def square_on_line(speed=CREEP_SPEED):
    """Creep until EACH eye sees the line, holding that side's wheel — pivots square."""
    # DriveBase can't move one wheel — this verb stays exactly as you built it.
    creep = mmps_to_degps(speed)
    left_done = right_done = False
    left_motor.run(creep)
    right_motor.run(creep)
    while not (left_done and right_done):
        if not left_done and left_eye.reflection() < LINE_THRESHOLD:
            left_motor.hold()
            left_done = True
        if not right_done and right_eye.reflection() < LINE_THRESHOLD:
            right_motor.hold()
            right_done = True
        wait(5)
```

:::

> 👀 **What to notice.** The run() and DriveBase tabs are **identical**. DriveBase has no
> per-wheel verb — it always moves both wheels as one robot — so when a job needs *one*
> wheel to stop on its own signal, even the D8 shortcut drops back down to the motors you
> already know. The shortcut doesn't replace your library; your library keeps what the
> shortcut can't do.

## Why we don't compete on dc()

::: {.callout-warning}
## dc() is a flashlight, not a vehicle

This bootcamp's missions run on `run()` and `DriveBase` **on purpose** (a deliberate
curriculum decision). `dc()` gives up the speed controller — the thing that makes your robot
do the same thing on a full battery at 9am and a tired one at 4pm. WRO runs are won by
*repeatability*, and repeatability is exactly what duty can't promise.

Read `my_lib_dc.py` to understand what your tools are doing underneath. Then close it and
drive `DriveBase`.
:::

## The full files (copy me) {#the-full-files-copy-me}

Each engine is one **self-contained** file. To run it: in the Pybricks editor, load **one
engine file**, then `from my_lib_run import *` (or `_dc` / `_drivebase`) in your program.
Downloads: [my_lib_run.py](code/compare/my_lib_run.py) ·
[my_lib_dc.py](code/compare/my_lib_dc.py) ·
[my_lib_drivebase.py](code/compare/my_lib_drivebase.py)

::: {.callout-note collapse="true"}
## my_lib_run.py — the library you built

```python
"""my_lib_run.py — YOUR library (built D5–D7), engine: motor.run().

run() regulates each wheel's SPEED for free (deg/s, battery-proof), but the
wheels don't know about each other — the gyro glue that keeps them honest
is yours: speed = error x KP.

Contract (identical in all three engines): distances in mm, speeds in mm/s,
headings in ABSOLUTE degrees (frame set by the last reset_heading).

Self-contained — tune every constant right here in this file.
KEEP IN SYNC with my_lib_dc.py / my_lib_drivebase.py — same names, same order.
"""

from pybricks.hubs import PrimeHub
from pybricks.parameters import Direction, Port
from pybricks.pupdevices import ColorSensor, Motor
from pybricks.tools import StopWatch, wait

# ---------------------------------------------- robot facts (tune here) --
LEFT_DRIVE_PORT = Port.A                       # re-port a motor = edit one line
RIGHT_DRIVE_PORT = Port.E
LEFT_SENSOR_PORT = Port.B
RIGHT_SENSOR_PORT = Port.F
LEFT_DRIVE_DIR = Direction.COUNTERCLOCKWISE     # flip if your build mirrors
RIGHT_DRIVE_DIR = Direction.CLOCKWISE
WHEEL_DIAMETER = 88    # mm — roll the wheel 5 turns, divide by 5*pi
STRAIGHT_SPEED = 300   # mm/s — default cruise
LINE_SPEED = 120       # mm/s while following a line
APPROACH_SPEED = 100   # mm/s for wall touches / scoring approaches
CREEP_SPEED = 60       # mm/s while squaring on a line
LINE_THRESHOLD = 50    # midpoint of black/white reflection calib
BACKOFF_MM = 50        # measured back-off after a wall touch
STALL_GRACE_MS = 300   # ignore the standing start before believing a stall

# ---------------------------------------------------------------- hardware --
hub = PrimeHub()
left_motor = Motor(LEFT_DRIVE_PORT, LEFT_DRIVE_DIR)
right_motor = Motor(RIGHT_DRIVE_PORT, RIGHT_DRIVE_DIR)
left_eye = ColorSensor(LEFT_SENSOR_PORT)
right_eye = ColorSensor(RIGHT_SENSOR_PORT)

# ------------------------------------------------------------ engine knobs --
KP_HEAD = 6           # deg/s of wheel-speed correction per deg heading error
KP_TURN = 3.5         # deg/s of wheel speed per deg of turn remaining
KP_LINE = 4           # deg/s per reflection point off the edge
KD_LINE = 12          # deg/s per reflection point of CHANGE — calms the wiggle  TUNE
MIN_TURN_SPEED = 35   # deg/s — slow enough to stop inside TURN_TOL  TUNE
MAX_TURN_SPEED = 400  # deg/s
TURN_TOL = 2          # deg — "close enough"


# ------------------------------------------------------------ unit helpers --
def mm_to_deg(mm):
    return mm * 360 / (3.142 * WHEEL_DIAMETER)


def mmps_to_degps(mmps):
    return mmps * 360 / (3.142 * WHEEL_DIAMETER)


# ------------------------------------------------------------------- verbs --
def reset_heading(heading_deg=0):
    """Zero (or set) the gyro heading frame."""
    hub.imu.reset_heading(heading_deg)


def drive_straight(distance_mm, speed=STRAIGHT_SPEED):
    """Gyro-held straight drive. Negative distance = backward."""
    wheel_speed = mmps_to_degps(speed)
    sign = 1 if distance_mm >= 0 else -1
    target_deg = mm_to_deg(abs(distance_mm))
    left_motor.reset_angle(0)
    right_motor.reset_angle(0)
    heading0 = hub.imu.heading()
    while (abs(left_motor.angle()) + abs(right_motor.angle())) / 2 < target_deg:
        steer = KP_HEAD * (hub.imu.heading() - heading0)  # speed = error x KP
        left_motor.run(sign * wheel_speed - steer)
        right_motor.run(sign * wheel_speed + steer)
        wait(10)
    left_motor.hold()
    right_motor.hold()


def turn_to(heading_deg):
    """Turn TO an absolute heading — a compass, not a steering wheel.
    turn_to(90) twice leaves you at 90, not 180."""
    while True:
        error = heading_deg - hub.imu.heading()
        if abs(error) < TURN_TOL:
            break
        # Speed SHRINKS with the error — big error turns fast, small error
        # creeps in, so the robot never coasts past the target.
        speed = min(MAX_TURN_SPEED, max(MIN_TURN_SPEED, KP_TURN * abs(error)))
        if error > 0:                       # positive heading = clockwise
            left_motor.run(speed)
            right_motor.run(-speed)
        else:
            left_motor.run(-speed)
            right_motor.run(speed)
        wait(5)
    left_motor.hold()
    right_motor.hold()


def drive_to_wall(speed=APPROACH_SPEED, timeout_ms=4000):
    """Wall touch WITHOUT a distance sensor. True on stall, False on timeout."""
    wheel_speed = mmps_to_degps(speed)
    watch = StopWatch()
    left_motor.run(wheel_speed)
    right_motor.run(wheel_speed)
    while watch.time() < timeout_ms:
        # run() keeps the speed controller on, so stalled() works for free —
        # the controller knows it can't reach its target. (dc() can't do this.)
        if (watch.time() > STALL_GRACE_MS
                and left_motor.stalled() and right_motor.stalled()):
            left_motor.stop()
            right_motor.stop()
            return True
        wait(10)
    left_motor.stop()
    right_motor.stop()
    return False


def wall_square(new_heading=0, backoff=BACKOFF_MM):
    """Square onto a wall, reset the gyro, back off a measured distance."""
    drive_to_wall()
    wait(250)                               # settle flat against the wall
    reset_heading(new_heading)
    drive_straight(-backoff)


def follow_line(sensor, distance_mm, speed=LINE_SPEED):
    """PD line follow on one sensor edge for an encoder distance."""
    base = mmps_to_degps(speed)
    target_deg = mm_to_deg(abs(distance_mm))
    left_motor.reset_angle(0)
    right_motor.reset_angle(0)
    last_error = 0
    while (abs(left_motor.angle()) + abs(right_motor.angle())) / 2 < target_deg:
        error = sensor.reflection() - LINE_THRESHOLD
        steer = KP_LINE * error + KD_LINE * (error - last_error)  # PD: P + D
        last_error = error
        left_motor.run(base + steer)
        right_motor.run(base - steer)
        wait(10)
    left_motor.hold()
    right_motor.hold()


def square_on_line(speed=CREEP_SPEED):
    """Creep until EACH eye sees the line, holding that side's wheel — pivots square."""
    creep = mmps_to_degps(speed)
    left_done = right_done = False
    left_motor.run(creep)
    right_motor.run(creep)
    while not (left_done and right_done):
        if not left_done and left_eye.reflection() < LINE_THRESHOLD:
            left_motor.hold()
            left_done = True
        if not right_done and right_eye.reflection() < LINE_THRESHOLD:
            right_motor.hold()
            right_done = True
        wait(5)
```
:::

::: {.callout-note collapse="true"}
## my_lib_dc.py — what's under the hood

```python
"""my_lib_dc.py — the SAME library, engine: motor.dc() (what's under the hood).

dc() sets raw motor power — duty %. Duty is NOT speed: a fresh battery, a
heavy load, a carpet seam all change what the same duty does. Everything
run() and DriveBase gave you for free is hand-rolled below.

Read this file to understand your tools. Do NOT compete on it — the bootcamp
keeps missions on run()/DriveBase on purpose.

Contract (identical in all three engines): distances in mm, speeds in mm/s,
headings in ABSOLUTE degrees (frame set by the last reset_heading).

Self-contained — tune every constant right here in this file.
KEEP IN SYNC with my_lib_run.py / my_lib_drivebase.py — same names, same order.
"""

from pybricks.hubs import PrimeHub
from pybricks.parameters import Direction, Port
from pybricks.pupdevices import ColorSensor, Motor
from pybricks.tools import StopWatch, wait

# ---------------------------------------------- robot facts (tune here) --
LEFT_DRIVE_PORT = Port.A                       # re-port a motor = edit one line
RIGHT_DRIVE_PORT = Port.E
LEFT_SENSOR_PORT = Port.B
RIGHT_SENSOR_PORT = Port.F
LEFT_DRIVE_DIR = Direction.COUNTERCLOCKWISE     # flip if your build mirrors
RIGHT_DRIVE_DIR = Direction.CLOCKWISE
WHEEL_DIAMETER = 88    # mm — roll the wheel 5 turns, divide by 5*pi
STRAIGHT_SPEED = 300   # mm/s — default cruise
LINE_SPEED = 120       # mm/s while following a line
APPROACH_SPEED = 100   # mm/s for wall touches / scoring approaches
CREEP_SPEED = 60       # mm/s while squaring on a line
LINE_THRESHOLD = 50    # midpoint of black/white reflection calib
BACKOFF_MM = 50        # measured back-off after a wall touch
STALL_GRACE_MS = 300   # ignore the standing start before believing a stall

# ---------------------------------------------------------------- hardware --
hub = PrimeHub()
left_motor = Motor(LEFT_DRIVE_PORT, LEFT_DRIVE_DIR)
right_motor = Motor(RIGHT_DRIVE_PORT, RIGHT_DRIVE_DIR)
left_eye = ColorSensor(LEFT_SENSOR_PORT)
right_eye = ColorSensor(RIGHT_SENSOR_PORT)

# ----------------------------------- engine knobs (the ones dc() hands YOU) --
DUTY_PER_MMS = 0.14   # duty % per mm/s ~ 100 / (970 deg/s * 276 mm / 360)
#                       drifts with battery + load — that's the dc() lesson  TUNE
MIN_DUTY = 12         # below this, friction wins and nothing moves  TUNE
MAX_DUTY = 90         # headroom left for the steering correction
RAMP_STEP = 3         # duty % added per 10 ms tick = hand-rolled acceleration
PRESS_DUTY = 20       # gentle lean while squaring on a wall
KP_HEAD = 2.5         # duty % per deg heading error  TUNE
KP_TURN = 1.2         # duty % per deg of turn remaining  TUNE
KP_LINE = 0.5         # duty % per reflection point off the edge  TUNE
KD_LINE = 1.5         # duty % per reflection point of CHANGE  TUNE
TURN_TOL = 2          # deg — "close enough"
STALL_SPEED = 20      # deg/s measured — below this = "stalled"  TUNE


# ------------------------------------------------------------ unit helpers --
def mm_to_deg(mm):
    return mm * 360 / (3.142 * WHEEL_DIAMETER)


def mmps_to_duty(mmps):
    return mmps * DUTY_PER_MMS              # a GUESS, not a promise


# ------------------------------------------------------------------- verbs --
def reset_heading(heading_deg=0):
    """Zero (or set) the gyro heading frame."""
    hub.imu.reset_heading(heading_deg)


def drive_straight(distance_mm, speed=STRAIGHT_SPEED):
    """Gyro-held straight drive. Negative distance = backward."""
    base = min(MAX_DUTY, max(MIN_DUTY, mmps_to_duty(speed)))
    sign = 1 if distance_mm >= 0 else -1
    target_deg = mm_to_deg(abs(distance_mm))
    left_motor.reset_angle(0)
    right_motor.reset_angle(0)
    heading0 = hub.imu.heading()
    duty = 0
    while (abs(left_motor.angle()) + abs(right_motor.angle())) / 2 < target_deg:
        duty = min(base, duty + RAMP_STEP)  # ramp up — no ramp = wheel chirp
        steer = KP_HEAD * (hub.imu.heading() - heading0)  # duty = error x KP
        # flip steer's sign if the robot fights its own correction  TUNE
        left_motor.dc(sign * duty - steer)
        right_motor.dc(sign * duty + steer)
        wait(10)
    left_motor.brake()
    right_motor.brake()


def turn_to(heading_deg):
    """Turn TO an absolute heading — a compass, not a steering wheel.
    turn_to(90) twice leaves you at 90, not 180."""
    while True:
        error = heading_deg - hub.imu.heading()
        if abs(error) < TURN_TOL:
            break
        # MIN_DUTY floor: pure error x KP fades below static friction near the
        # target and the turn stalls a few degrees short — forever. The floor
        # is the honest fix (no I-term in this bootcamp).
        duty = min(MAX_DUTY, max(MIN_DUTY, KP_TURN * abs(error)))
        if error > 0:                       # positive heading = clockwise
            left_motor.dc(duty)
            right_motor.dc(-duty)
        else:
            left_motor.dc(-duty)
            right_motor.dc(duty)
        wait(5)
    left_motor.brake()
    right_motor.brake()
    wait(150)                               # settle — brake is not hold


def drive_to_wall(speed=APPROACH_SPEED, timeout_ms=4000):
    """Wall touch WITHOUT a distance sensor. True on stall, False on timeout."""
    duty = max(MIN_DUTY, mmps_to_duty(speed))
    watch = StopWatch()
    left_motor.dc(duty)
    right_motor.dc(duty)
    while watch.time() < timeout_ms:
        # stalled() means nothing under dc() — no controller, no target.
        # The encoder is the sensor: measured speed collapsing = the wall.
        if (watch.time() > STALL_GRACE_MS
                and abs(left_motor.speed()) < STALL_SPEED
                and abs(right_motor.speed()) < STALL_SPEED):
            left_motor.brake()
            right_motor.brake()
            return True
        wait(10)
    left_motor.brake()
    right_motor.brake()
    return False


def wall_square(new_heading=0, backoff=BACKOFF_MM):
    """Square onto a wall, reset the gyro, back off a measured distance."""
    duty = max(MIN_DUTY, mmps_to_duty(APPROACH_SPEED))
    watch = StopWatch()
    left_motor.dc(duty)
    right_motor.dc(duty)
    left_done = right_done = False
    while not (left_done and right_done):
        if watch.time() > 4000:
            break
        if watch.time() > STALL_GRACE_MS:
            # Each wheel that reaches the wall drops to a gentle lean so it
            # keeps pressing while the other wheel catches up = square.
            if not left_done and abs(left_motor.speed()) < STALL_SPEED:
                left_motor.dc(PRESS_DUTY)
                left_done = True
            if not right_done and abs(right_motor.speed()) < STALL_SPEED:
                right_motor.dc(PRESS_DUTY)
                right_done = True
        wait(10)
    wait(250)                               # settle flat against the wall
    reset_heading(new_heading)
    left_motor.brake()
    right_motor.brake()
    drive_straight(-backoff)


def follow_line(sensor, distance_mm, speed=LINE_SPEED):
    """PD line follow on one sensor edge for an encoder distance."""
    base = max(MIN_DUTY, mmps_to_duty(speed))
    target_deg = mm_to_deg(abs(distance_mm))
    left_motor.reset_angle(0)
    right_motor.reset_angle(0)
    last_error = 0
    while (abs(left_motor.angle()) + abs(right_motor.angle())) / 2 < target_deg:
        error = sensor.reflection() - LINE_THRESHOLD
        steer = KP_LINE * error + KD_LINE * (error - last_error)  # PD — raw duty:
        last_error = error                  # visibly slows on seams (duty != speed)
        left_motor.dc(min(MAX_DUTY, max(-MAX_DUTY, base + steer)))
        right_motor.dc(min(MAX_DUTY, max(-MAX_DUTY, base - steer)))
        wait(10)
    left_motor.brake()
    right_motor.brake()


def square_on_line(speed=CREEP_SPEED):
    """Creep until EACH eye sees the line, holding that side's wheel — pivots square."""
    creep = max(MIN_DUTY, mmps_to_duty(speed))
    left_done = right_done = False
    left_motor.dc(creep)
    right_motor.dc(creep)
    while not (left_done and right_done):
        if not left_done and left_eye.reflection() < LINE_THRESHOLD:
            left_motor.hold()               # hold() is a controller command —
            left_done = True                # it works fine even from dc()
        if not right_done and right_eye.reflection() < LINE_THRESHOLD:
            right_motor.hold()
            right_done = True
        wait(5)
```
:::

::: {.callout-note collapse="true"}
## my_lib_drivebase.py — the earned shortcut

```python
"""my_lib_drivebase.py — the SAME library, engine: DriveBase (the D8 shortcut).

Same seven verbs, now driving Pybricks' professional controller: gyro-fused,
battery-proof, ramped. Count the lines that disappeared — then notice which
verb did NOT: DriveBase has no per-wheel verb, so square_on_line still drops
to the motors. The library keeps what the shortcut can't do.

Contract (identical in all three engines): distances in mm, speeds in mm/s,
headings in ABSOLUTE degrees (frame set by the last reset_heading).

Self-contained — tune every constant right here in this file.
KEEP IN SYNC with my_lib_run.py / my_lib_dc.py — same names, same order.
"""

from pybricks.hubs import PrimeHub
from pybricks.parameters import Direction, Port
from pybricks.pupdevices import ColorSensor, Motor
from pybricks.robotics import DriveBase
from pybricks.tools import StopWatch, wait

# ---------------------------------------------- robot facts (tune here) --
LEFT_DRIVE_PORT = Port.A                       # re-port a motor = edit one line
RIGHT_DRIVE_PORT = Port.E
LEFT_SENSOR_PORT = Port.B
RIGHT_SENSOR_PORT = Port.F
LEFT_DRIVE_DIR = Direction.COUNTERCLOCKWISE     # flip if your build mirrors
RIGHT_DRIVE_DIR = Direction.CLOCKWISE
WHEEL_DIAMETER = 88    # mm — roll the wheel 5 turns, divide by 5*pi
AXLE_TRACK = 145       # mm — distance between wheel CONTACT centers
STRAIGHT_SPEED = 300   # mm/s — default cruise
STRAIGHT_ACCEL = 450   # mm/s^2
TURN_RATE = 150        # deg/s
TURN_ACCEL = 300       # deg/s^2
LINE_SPEED = 120       # mm/s while following a line
APPROACH_SPEED = 100   # mm/s for wall touches / scoring approaches
CREEP_SPEED = 60       # mm/s while squaring on a line
LINE_THRESHOLD = 50    # midpoint of black/white reflection calib
BACKOFF_MM = 50        # measured back-off after a wall touch
STALL_GRACE_MS = 300   # ignore the standing start before believing a stall

# ---------------------------------------------------------------- hardware --
hub = PrimeHub()
left_motor = Motor(LEFT_DRIVE_PORT, LEFT_DRIVE_DIR)
right_motor = Motor(RIGHT_DRIVE_PORT, RIGHT_DRIVE_DIR)
left_eye = ColorSensor(LEFT_SENSOR_PORT)
right_eye = ColorSensor(RIGHT_SENSOR_PORT)

drive = DriveBase(left_motor, right_motor, WHEEL_DIAMETER, AXLE_TRACK)
drive.use_gyro(True)
drive.settings(
    straight_speed=STRAIGHT_SPEED,
    straight_acceleration=STRAIGHT_ACCEL,
    turn_rate=TURN_RATE,
    turn_acceleration=TURN_ACCEL,
)

# ------------------------------------------------------- line-follow gains --
KP_LINE = 1.2         # steering rate per reflection point off the edge
KD_LINE = 4.0         # steering rate per reflection point of CHANGE  TUNE
# (drive.settings above handles ramp + gyro — no engine-compensation knobs.)


# ------------------------------------------------------------ unit helpers --
def mmps_to_degps(mmps):
    # DriveBase converts units everywhere else — only the per-wheel
    # square_on_line still needs this by hand.
    return mmps * 360 / (3.142 * WHEEL_DIAMETER)


# ------------------------------------------------------------------- verbs --
def reset_heading(heading_deg=0):
    """Zero (or set) the gyro heading frame."""
    hub.imu.reset_heading(heading_deg)


def drive_straight(distance_mm, speed=STRAIGHT_SPEED):
    """Gyro-held straight drive. Negative distance = backward."""
    drive.settings(straight_speed=speed)
    drive.straight(distance_mm)


def turn_to(heading_deg):
    """Turn TO an absolute heading — a compass, not a steering wheel.
    turn_to(90) twice leaves you at 90, not 180.

    drive.turn() is RELATIVE — it turns BY the angle you hand it. So the
    subtraction below is what makes this verb absolute. Without it the turns
    pile up and turn_to(90) three times ends at 270."""
    drive.turn(heading_deg - hub.imu.heading())


def drive_to_wall(speed=APPROACH_SPEED, timeout_ms=4000):
    """Wall touch WITHOUT a distance sensor. True on stall, False on timeout."""
    watch = StopWatch()
    drive.drive(speed, 0)
    while watch.time() < timeout_ms:
        # stall detection: built in. The grace window still matters — a
        # standing start looks exactly like a wall for the first moment.
        if watch.time() > STALL_GRACE_MS and drive.stalled():
            drive.stop()
            return True
        wait(10)
    drive.stop()
    return False


def wall_square(new_heading=0, backoff=BACKOFF_MM):
    """Square onto a wall, reset the gyro, back off a measured distance."""
    drive_to_wall()
    wait(250)                               # settle flat against the wall
    reset_heading(new_heading)
    drive_straight(-backoff)


def follow_line(sensor, distance_mm, speed=LINE_SPEED):
    """PD line follow on one sensor edge for an encoder distance."""
    drive.reset()
    last_error = 0
    while abs(drive.distance()) < abs(distance_mm):
        error = sensor.reflection() - LINE_THRESHOLD
        steer = KP_LINE * error + KD_LINE * (error - last_error)  # PD: P + D
        last_error = error
        drive.drive(speed, steer)
        wait(10)
    drive.stop()


def square_on_line(speed=CREEP_SPEED):
    """Creep until EACH eye sees the line, holding that side's wheel — pivots square."""
    # DriveBase can't move one wheel — this verb stays exactly as you built it.
    creep = mmps_to_degps(speed)
    left_done = right_done = False
    left_motor.run(creep)
    right_motor.run(creep)
    while not (left_done and right_done):
        if not left_done and left_eye.reflection() < LINE_THRESHOLD:
            left_motor.hold()
            left_done = True
        if not right_done and right_eye.reflection() < LINE_THRESHOLD:
            right_motor.hold()
            right_done = True
        wait(5)
```
:::

## 🎸 What pro teams do (beyond this bootcamp)

::: {.callout-tip collapse="true"}
## Five ideas from competition libraries — read for taste, not for homework

Big WRO teams ship libraries 10× this size (one public example: *HYPERLIBwro*, ~1,000 lines).
Almost everything extra is variations on five ideas — all of which you already have the
pieces for:

**1 — Trapezoid ramping.** Our dc() file ramps *up* (`RAMP_STEP`); pros also ramp *down*
before the target so the robot never slams into its own stop:

```python
max_change = MAX_ACC * (LOOP_MS / 1000)      # biggest speed step per tick
if abs(wanted - current) > max_change:
    wanted = current + max_change * (1 if wanted > current else -1)
```

**2 — PD on the heading.** We hold heading with P; a D term reacts to how *fast* the error
is changing and calms the wiggle at speed:

```python
correction = KP * error + KD * (error - last_error)
last_error = error
```

Pro numbers like `Kp=50, Kd=780` look huge only because their duty scale and loop tick
differ — the *shape* is exactly your two-line controller.

**3 — Angle wrapping.** Ask for `turn_to(350)` from heading 0 and a naive turn goes the
long way. One line picks the short way around:

```python
error = (error + 180) % 360 - 180            # 350 becomes -10
```

**4 — Speed-scheduled KP.** One KP can't love two speeds: crisp at 120 mm/s is twitchy at
300 mm/s. Scale it with speed:

```python
kp = KP_SLOW + (KP_FAST - KP_SLOW) * (speed - MIN_S) / (MAX_S - MIN_S)
```

**5 — Two-point calibration.** Instead of one `line_threshold`, measure black and white
once and rescale every reading to 0…1 — thresholds stop caring about ambient light:

```python
level = (sensor.reflection() - BLACK) / (WHITE - BLACK)   # 0 = line, 1 = mat
```

**What's beyond: driving by coordinates.** Pro libraries also track the robot's (x, y) from
the encoders + gyro (*odometry*) and steer to field points (`MoveToPoint`, Pure Pursuit).
This bootcamp *deliberately* doesn't: odometry accumulates error every mm, and our missions
erase error the honest way — `wall_square()`. Even pro teams re-zero on walls. It's a
horizon, not a homework.
:::
